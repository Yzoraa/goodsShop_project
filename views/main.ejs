<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>메인화면</title>
    <link rel="stylesheet" href="/public/css/main.css">
</head>
<body>
    <div class="topBox">
        <div class="topLeft">
            <img class="logoText" src="/img/textLogo.jpg" width="220px">
        </div>
        <div class="topRight">
            <div class="text">LOGIN</div>
            <div>|</div>
            <div class="text">MYPAGE</div>
            <div>|</div>
            <div class="text" onclick="cartPage()">CART</div>
        </div>
    </div>

    <div class="sliderContainer">
        <div class="slider"></div>
        <div class="indicators"></div>
        <script src="/public/js/slider.js"></script>
    </div>

    <div class="bodyText">BEST SELLER</div>
    <div class="productCNT">등록제품 : <% getMains.length %></div><br />
    <div class="bodyWrap">
        <% getMains.forEach(product => { %>
            <div class="card" onclick="detailPage(<%= product.id %>)">
                <img src="<%= product.img_url %>" alt="<%= product.name %>" class="cardImg">
                <img src="/img/heartBlack.png" alt="좋아요" class="heartIcon">

                <span class="cardTitle"><%= product.name %></span>
            </div>
        <% }); %>
    </div>
</body>
<script src="/public/js/main.js"></script>
</html>